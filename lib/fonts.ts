import { Space_Grotesk, Newsreader, Inter } from "next/font/google"

// Font options to audition for the intro (and the rest of the page).
export const grotesk = Space_Grotesk({ subsets: ["latin"], display: "swap" })
export const serif = Newsreader({ subsets: ["latin"], display: "swap" })
export const inter = Inter({ subsets: ["latin"], display: "swap" })
