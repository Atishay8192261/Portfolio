/**
 * Flat soft near-black — leerob's exact dark background (#1a1a1a). No glow, no
 * animation; the calm flatness is the tone.
 */
export function Background() {
  return <div className="fixed inset-0 -z-10 bg-[#1a1a1a]" />
}
